/*
Напиши JavaScript програма која пресметува сметка за струја.

Правила:

Потрошени се 350 kWh

Цена по kWh:

До 300 kWh → 0.08 €

Над 300 kWh → 0.12 €

Ако потрошувачката е над 300 kWh, користи повисока цена

Прикажи ја вкупната сума

👉 Користи if / else за да одлучиш која цена да се примени. 
*/
// Код од барањето
/*
const usedKWh = 350;
const pricePerKWh = usedKWh <= 300 ? 0.08 : 0.12;
const totalAmount = usedKWh * pricePerKWh;
alert(`Вкупната сума за струја е: ${totalAmount.toFixed(2)} €`);
*/

// Код со промпт
const usedKWh = prompt("Внесете ја потрошената количина на kWh:");
let pricePerKWh;
if (usedKWh <= 300) {
    pricePerKWh = 0.08;
} else {
    pricePerKWh = 0.12;
}   

const totalAmount = usedKWh * pricePerKWh;
alert(`Вкупната сума за струја е: ${totalAmount.toFixed(2)} €`);